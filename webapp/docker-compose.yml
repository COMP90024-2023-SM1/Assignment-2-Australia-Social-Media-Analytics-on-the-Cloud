version: "3"
services:
  # postgres:
  #   image: "postgres:latest"
  #   environment:
  #     - POSTGRES_PASSWORD=postgres_password
  # couchdb:
  #   image: "couchdb:latest"
  #   environment:
  #     - COUCHDB_USER=admin
  #     - COUCHDB_PASSWORD=admin
  #   ports:
  #     - "5984:5984“

  nginx:
    depends_on:
      - api
      - client
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
      - "80:80"
  api:
    build:
      dockerfile: Dockerfile.dev
      context: "./server"
    volumes:
      - /app/node_modules
      - ./server:/app
    environment:
       - DB_URL="http://admin:admin@172.26.128.113:5984"
  client:
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    build:
      dockerfile: Dockerfile.dev
      context: ./client
    volumes:
      - /app/node_modules
      - ./client:/app
